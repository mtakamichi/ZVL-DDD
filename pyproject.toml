[project]
name = "zvl-ddd"
version = "0.1.0"
description = "Zero-shot distracted driver detection via vision-language models with double decoupling"
readme = "README.md"
requires-python = ">=3.10,<3.13"
license = { text = "Apache-2.0" }
authors = [
  { name = "Takamichi Miyata" }
]

dependencies = [
  "numpy>=2.1,<3",
  "pandas>=2.3,<3",
  "pillow>=11.3,<12",
  "scikit-learn>=1.7,<2",
  "scipy>=1.15,<2",
  "tqdm>=4.67,<5",
  "ftfy>=6.3,<7",
  "regex>=2025.9",
  "requests>=2.32,<3",
  "torch==2.6.0",
  "torchvision==0.21.0",
  "clip @ git+https://github.com/openai/CLIP.git@dcba3cb2e2827b402d2701e7e1c7d9fed8a20ef1",
  # The torch 2.6.0+cu126 wheels do not bundle the CUDA runtime libraries; they
  # are provided by these nvidia-* packages on Linux/x86_64. The platform markers
  # exclude them elsewhere (e.g. CPU-only macOS), where torch uses its CPU build.
  "nvidia-cublas-cu12==12.6.4.1; platform_system == 'Linux' and platform_machine == 'x86_64'",
  "nvidia-cuda-cupti-cu12==12.6.80; platform_system == 'Linux' and platform_machine == 'x86_64'",
  "nvidia-cuda-nvrtc-cu12==12.6.77; platform_system == 'Linux' and platform_machine == 'x86_64'",
  "nvidia-cuda-runtime-cu12==12.6.77; platform_system == 'Linux' and platform_machine == 'x86_64'",
  "nvidia-cudnn-cu12==9.5.1.17; platform_system == 'Linux' and platform_machine == 'x86_64'",
  "nvidia-cufft-cu12==11.3.0.4; platform_system == 'Linux' and platform_machine == 'x86_64'",
  "nvidia-curand-cu12==10.3.7.77; platform_system == 'Linux' and platform_machine == 'x86_64'",
  "nvidia-cusolver-cu12==11.7.1.2; platform_system == 'Linux' and platform_machine == 'x86_64'",
  "nvidia-cusparse-cu12==12.5.4.2; platform_system == 'Linux' and platform_machine == 'x86_64'",
  "nvidia-cusparselt-cu12==0.6.3; platform_system == 'Linux' and platform_machine == 'x86_64'",
  "nvidia-nccl-cu12==2.21.5; platform_system == 'Linux' and platform_machine == 'x86_64'",
  "nvidia-nvjitlink-cu12==12.6.85; platform_system == 'Linux' and platform_machine == 'x86_64'",
  "nvidia-nvtx-cu12==12.6.77; platform_system == 'Linux' and platform_machine == 'x86_64'",
]

[dependency-groups]
dev = [
  "ruff>=0.8"
]

[tool.uv]
package = false

[tool.uv.extra-build-dependencies]
clip = ["setuptools<81"]

[tool.uv.sources]
torch = [
  { index = "pytorch-cu126" }
]
torchvision = [
  { index = "pytorch-cu126" }
]

[[tool.uv.index]]
name = "pytorch-cu126"
url = "https://download.pytorch.org/whl/cu126"
explicit = true

[tool.ruff]
line-length = 100
target-version = "py310"